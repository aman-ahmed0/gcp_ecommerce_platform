apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: ecommerce
spec:
  clusterIP: None            # This makes it a headless service
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432